import { makeStyles } from "@material-ui/core/styles";

const cardStyles = makeStyles({
  root: {
    width: "60%",
    height: "95%",
    marginTop: "2%",
  },
  title: {
    fontSize: 30,
  },
  pos: {
    marginBottom: 12,
  },
});

export default cardStyles;